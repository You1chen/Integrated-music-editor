# PyQt6 界面入门 —— 从零开始

> 写给会 Python 但没接触过 GUI 编程的人。读完就能自己改界面。

---

## 目录

1. [什么是 GUI 程序](#1-什么是-gui-程序)
2. [Qt 的世界观：万物皆控件](#2-qt-的世界观万物皆控件)
3. [控件长什么样 —— 认识常见控件](#3-控件长什么样--认识常见控件)
4. [布局：决定控件放在哪里](#4-布局决定控件放在哪里)
5. [控件树：父与子](#5-控件树父与子)
6. [大小怎么定](#6-大小怎么定)
7. [QSS：给控件化妆](#7-qss给控件化妆)
8. [本项目结构速览](#8-本项目结构速览)
9. [实操：改一个现成的界面](#9-实操改一个现成的界面)
10. [调试技巧](#10-调试技巧)

---

## 1. 什么是 GUI 程序

GUI = Graphical User Interface，就是有窗口、有按钮、能点击的程序。

你平时写的 Python 脚本：
```python
name = input("请输入名字：")   # 在黑框框里打字
print(f"你好 {name}")
```

GUI 版本长这样：
```
┌──────────────────────┐
│  你好                │
│  ┌──────────────┐    │
│  │ 请输入名字     │    │
│  └──────────────┘    │
│  ┌────────┐          │
│  │  确定   │          │
│  └────────┘          │
└──────────────────────┘
```

**GUI 程序在内部跑着一个死循环：**

```python
while True:
    检测鼠标点击、键盘按下
    把事件交给对应的控件处理
    重新绘制界面
```

这个死循环叫**事件循环**。Qt 已经帮你写好了，你不需要自己写。你只需要做三件事：

1. 创建控件（按钮、输入框、标签）
2. 用布局摆放控件
3. 给控件绑定"点击后干什么"

---

## 2. Qt 的世界观：万物皆控件

在 Qt 里，**你在窗口里看到的一切都是控件（Widget）**：

```
┌──────────────────────────────────┐
│  QMainWindow（窗口本身）          │
│  ┌────────────────────────────┐  │
│  │  文字标签 ← QLabel         │  │
│  │  ┌──────────────────┐      │  │
│  │  │ 输入框 ← QLineEdit│      │  │
│  │  └──────────────────┘      │  │
│  │  ┌──────────┐              │  │
│  │  │ 按钮 ← QPushButton     │  │
│  │  └──────────┘              │  │
│  └────────────────────────────┘  │
└──────────────────────────────────┘
```

- 窗口本身是控件
- 按钮是控件
- 文字标签是控件
- 输入框是控件

但是**布局不是控件**。布局是透明的、看不见的，只管一件事：**把控件摆在正确的位置**。

```
控件 = 看得见的东西（按钮、文字、输入框）
布局 = 看不见的排版规则（谁在谁上面、间距多少）
```

这是最重要的基础概念。一句话记住：**控件负责"是什么"，布局负责"放哪里"。**

---

## 3. 控件长什么样 —— 认识常见控件

### 3.1 QMainWindow —— 窗口

```python
from PyQt6.QtWidgets import QMainWindow

window = QMainWindow()
window.setWindowTitle("我的应用")   # 标题栏文字
window.resize(800, 600)             # 初始大小
window.setMinimumSize(400, 300)     # 最小尺寸（用户不能缩得更小）
```

窗口是最高层的容器，里面装其他一切。

### 3.2 QLabel —— 显示文字或图片

```python
from PyQt6.QtWidgets import QLabel

label = QLabel("你好世界")
label.setText("新的文字")           # 修改文字
label.setWordWrap(True)             # 自动换行
```

**纯展示，用户不能编辑。** 类似 HTML 的 `<span>` 或 `<p>`。

### 3.3 QPushButton —— 按钮

```python
from PyQt6.QtWidgets import QPushButton

btn = QPushButton("点我")
btn.clicked.connect(处理函数)        # 点击后干什么
```

### 3.4 QLineEdit —— 单行输入框

```python
from PyQt6.QtWidgets import QLineEdit

inp = QLineEdit()
inp.setPlaceholderText("请输入歌名")  # 灰色提示文字
text = inp.text()                     # 获取用户输入
```

### 3.5 QPlainTextEdit —— 多行文本框

```python
from PyQt6.QtWidgets import QPlainTextEdit

editor = QPlainTextEdit()
editor.setPlainText("第一行\n第二行")
text = editor.toPlainText()
```

### 3.6 QCheckBox —— 勾选框

```python
cb = QCheckBox("启用此功能")
cb.setChecked(True)        # 默认勾选
print(cb.isChecked())      # True/False
```

### 3.7 QGroupBox —— 带标题的框

```python
group = QGroupBox("封面图片")
inner = QVBoxLayout(group)
inner.addWidget(封面控件)
```

效果：
```
┌─ 封面图片 ─────────────┐
│  [封面缩略图]           │
└────────────────────────┘
```

### 3.8 QScrollArea —— 可滚动区域

```python
scroll = QScrollArea()
scroll.setWidgetResizable(True)
scroll.setWidget(装不下的内容)
```

内容超出窗口时，自动出现滚动条。

### 3.9 QDialog —— 弹窗

```python
dialog = QDialog(parent)
dialog.setWindowTitle("裁剪图片")
dialog.resize(600, 500)

layout = QVBoxLayout(dialog)
layout.addWidget(QLabel("内容..."))

if dialog.exec() == QDialog.DialogCode.Accepted:
    print("用户点了确定")
```

### 速查表：控件 vs HTML

| Qt 控件 | 做什么的 | 像 HTML 的什么 |
|---------|---------|---------------|
| `QMainWindow` | 主窗口 | `<html>` |
| `QLabel` | 显示文字/图片 | `<span>` `<p>` `<img>` |
| `QPushButton` | 按钮 | `<button>` |
| `QLineEdit` | 单行输入 | `<input type="text">` |
| `QPlainTextEdit` | 多行输入 | `<textarea>` |
| `QCheckBox` | 勾选框 | `<input type="checkbox">` |
| `QRadioButton` | 单选按钮 | `<input type="radio">` |
| `QComboBox` | 下拉选择 | `<select>` |
| `QGroupBox` | 分组框 | `<fieldset>` |
| `QScrollArea` | 滚动区 | `overflow: scroll` |
| `QDialog` | 弹窗 | modal |

---

## 4. 布局：决定控件放在哪里

### 4.1 没有布局会怎样

```python
# 错误示范：不设布局
btn1 = QPushButton("A", window)
btn2 = QPushButton("B", window)
btn3 = QPushButton("C", window)
```

三个按钮全部叠在左上角，互相遮盖。因为没有人告诉它们"该怎么排"。

### 4.2 加上布局

```python
layout = QVBoxLayout(window)
layout.addWidget(btn1)
layout.addWidget(btn2)
layout.addWidget(btn3)
```

结果：三个按钮整整齐齐竖着排。

### 4.3 三种基础布局

```python
# 垂直堆叠
QVBoxLayout()
```
```
┌───┐
│ A │
│ B │
│ C │
└───┘
```

```python
# 水平排列
QHBoxLayout()
```
```
┌───────────┐
│ A   B   C │
└───────────┘
```

```python
# 左标签右输入（表单）
QFormLayout()
```
```
┌──────────────┐
│ 歌名: [____] │
│ 歌手: [____] │
└──────────────┘
```

### 4.4 布局嵌套 —— 做出复杂界面的核心技巧

```python
# 整体竖着排
outer = QVBoxLayout()

# 第一行：横着排（封面 + 信息）
row1 = QHBoxLayout()
row1.addWidget(封面)
row1.addWidget(信息)
outer.addLayout(row1)              # ← 把横排塞进竖排

# 第二行：横着排（两个按钮靠右）
row2 = QHBoxLayout()
row2.addStretch()                  # 弹簧，把按钮推到右边
row2.addWidget(取消)
row2.addWidget(确定)
outer.addLayout(row2)
```

效果：
```
┌─────────────────────────┐
│ [封面]  歌曲信息...     │  ← QHBoxLayout
│                         │
│           [取消] [确定] │  ← QHBoxLayout + stretch
└─────────────────────────┘
```

**布局可以无限嵌套，这是做出任何界面的唯一方法。**

### 4.5 四个调间距的参数

```python
layout.setContentsMargins(左, 上, 右, 下)  # 四边留白（像素）
layout.setSpacing(12)                        # 子元素之间的间距
layout.addStretch()                          # 弹簧（占满剩余空间）
```

- `setContentsMargins(24, 16, 24, 16)` → 上边留 16px 空白，其他三边 24px
- `setSpacing(12)` → 每个按钮之间间隔 12px
- `addStretch()` → 一个能无限伸缩的空白占位符

### 4.6 addStretch 图解

```python
# 靠右
row.addStretch()
row.addWidget(btn)
```
```
┌──────────────────┐
│            [btn] │
└──────────────────┘
弹簧吃掉左边所有空间，按钮被挤到右边
```

```python
# 居中
row.addStretch()
row.addWidget(btn)
row.addStretch()
```
```
┌──────────────────┐
│       [btn]      │
└──────────────────┘
两边各一个弹簧，按钮在正中间
```

```python
# 一左一右
row.addWidget(取消)
row.addStretch()
row.addWidget(确定)
```
```
┌──────────────────┐
│ [取消]    [确定] │
└──────────────────┘
弹簧夹在中间，左右推开
```

---

## 5. 控件树：父与子

### 5.1 一切控件都有父子关系

```
QMainWindow（爷爷）
  └── centralWidget（爸爸——窗口的内容区）
        └── QVBoxLayout（妈妈——排版规则）
              ├── QLabel    （孩子）
              ├── QLineEdit （孩子）
              └── QPushButton（孩子）
```

**父控件** 决定了子控件显示在谁的范围里。
**布局** 决定了子控件在父控件内部的具体位置。

### 5.2 建立父子关系

```python
# 方式一：构造时传参
btn = QPushButton("按钮", parent_widget)

# 方式二：通过布局间接建立（更常用）
layout = QVBoxLayout(parent_widget)
layout.addWidget(btn)    # btn 自动认 parent_widget 当父亲
```

### 5.3 三条重要规则

- 子控件只能显示在父控件的矩形范围内
- 父控件被销毁时，所有子控件自动销毁
- 父控件被隐藏时，子控件一起隐藏

---

## 6. 大小怎么定

### 6.1 决定大小的四个角色

控件最终多大，由四方博弈决定：

```
① 控件自己说 → "我大概需要这么大"     （sizeHint）
② 布局说     → "但你只能在这个空间里" （布局约束）
③ 你说       → "不能小于 X，不能大于 Y"（setFixedSize / setMinimumSize）
④ QSS 说     → "min-width: 100px"     （样式表）
```

### 6.2 你出手干预

```python
# 钉死（不管窗口怎么缩放都不变）
btn.setFixedSize(140, 140)    # 固定 140×140
btn.setFixedWidth(200)        # 只固定宽度
btn.setFixedHeight(48)        # 只固定高度

# 限制范围（允许伸缩，但有上下限）
btn.setMinimumWidth(100)      # 不能比 100 窄
btn.setMaximumHeight(200)     # 不能比 200 高
```

### 6.3 QScrollArea：内容太长时自动滚动

```python
scroll = QScrollArea()
scroll.setWidgetResizable(True)
scroll.setWidget(很长的内容)

# 使用场景：
# - 设置页（选项太多，窗口装不下）
# - 元信息编辑页（字段太多）
# - 任何内容可能超出屏幕的地方
```

不加 `QScrollArea`，多出来的部分直接被裁掉，用户永远看不到。

---

## 7. QSS：给控件化妆

### 7.1 QSS 就是 CSS

如果你会 CSS，QSS 白送——语法几乎一样。唯一的区别是选择器写的是 Qt 控件名而不是 HTML 标签名。

```css
QPushButton {
    background-color: #ff691f;   /* 背景色        */
    color: white;                /* 文字颜色      */
    border: 1px solid #ccc;      /* 边框          */
    border-radius: 8px;          /* 圆角          */
    padding: 6px 14px;           /* 内边距        */
    font-size: 14px;             /* 字号          */
}

QPushButton:hover {              /* 鼠标悬停时    */
    background-color: #e85d12;
}

QPushButton:pressed {            /* 按住不放时    */
    background-color: #cc5200;
}
```

### 7.2 两种用法

**全局**——影响整个程序里所有同类控件：

```python
app.setStyleSheet("QPushButton { background: orange; }")
# 之后每新建一个 QPushButton 都是橙色
```

**内联**——只影响这一个：

```python
btn.setStyleSheet("background: red; color: white;")
# 只有 btn 变红
```

内联优先于全局，和 CSS 的优先级规则一样。

### 7.3 最常用的 QSS 属性

```css
/* ── 颜色 ── */
background-color: #ff691f;
background: transparent;       /* 透明背景 */
color: #ffffff;                /* 文字颜色 */

/* ── 边框 ── */
border: 2px solid red;         /* 四边统一 */
border-bottom: 2px solid red;  /* 只要底边 */
border: none;                  /* 不要边框 */
border-radius: 8px;            /* 圆角 */

/* ── 间距 ── */
padding: 10px;                 /* 四边一样 */
padding: 6px 14px;             /* 上下 6，左右 14 */

/* ── 文字 ── */
font-size: 14px;
font-weight: bold;             /* bold / normal */
font-family: "Microsoft YaHei", sans-serif;
```

### 7.4 只给特定的控件设置样式

```css
#saveButton {                   /* objectName="saveButton" 的控件 */
    background: green;
}

QPushButton:checked {           /* toggle 按钮选中态 */
    background: blue;
}

QPushButton:disabled {          /* 禁用态 */
    opacity: 0.5;
}
```

代码中设置：
```python
btn.setObjectName("saveButton")
```

---

## 8. 本项目结构速览

### 8.1 全局样式在哪

**`src/ui/content_stack.py`** → `apply_theme()` 函数

这个函数里包含了所有控件的默认外观。改这里会改变整个应用。

### 8.2 主题变量

全局 QSS 中颜色不是写死的，用了变量适配亮色/暗色模式：

| 变量 | 含义 | 亮色 | 暗色 |
|------|------|------|------|
| `{bg}` | 页面背景色 | `#f1f3f4` | `#111111` |
| `{fg}` | 文字颜色 | `#111111` | `#eeeeee` |
| `{theme_color}` | 主题色 | 橙色 | 橙色 |
| `{contrast_text}` | 主题色上的字 | 自动黑白 | 自动黑白 |

### 8.3 页面文件对应关系

| 文件 | 标签页 |
|------|--------|
| `src/ui/home_page.py` | 主页 |
| `src/ui/synchronizer_page.py` | 歌词制作 |
| `src/ui/meta_editor_page.py` | 编辑元信息 |
| `src/ui/preferences_page.py` | 设置 |
| `src/ui/editor_page.py` | （隐藏页，拖放歌词时弹出） |

每个页面都是 `QWidget` 的一个子类，在 `main.py` 里创建并注册。

### 8.4 一个页面的标准骨架

```python
class MyPage(QScrollArea):
    def __init__(self, main_window):
        super().__init__()

        container = QWidget()                     # ① 创建容器
        self.setWidget(container)

        layout = QVBoxLayout(container)           # ② 装上布局
        layout.setContentsMargins(24, 16, 24, 16) # ③ 设边距
        layout.setSpacing(12)                     # ④ 设间距

        label = QLabel("标题")                    # ⑤ 创建控件
        layout.addWidget(label)                   # ⑥ 放进布局

        btn = QPushButton("按钮")
        btn.clicked.connect(self._on_click)       # ⑦ 绑定事件
        layout.addWidget(btn)

        layout.addStretch()                       # ⑧ 弹簧收尾

    def _on_click(self):
        print("点了")
```

---

## 9. 实操：边改边看效果

### 练习一：改间距

打开 `src/ui/meta_editor_page.py`，搜 `setSpacing(12)`，把 `12` 改成 `4` → 重启 → 全部挤在一起。改成 `30` → 重启 → 松散得不行。

### 练习二：改颜色

打开 `src/ui/content_stack.py`，搜 `QPushButton`，找到 `border-radius`，改成 `20px` → 重启 → 所有按钮变圆。

### 练习三：改大小

在 `meta_editor_page.py` 搜 `setFixedSize(140, 140)`，改成 `(200, 200)` → 重启 → 封面变大了。

### 练习四：改位置

找一段有按钮的代码，在按钮前面插入：
```python
layout.addStretch()
```
按钮从左边跑到了右边。

---

## 10. 调试技巧

### 加背景色看边界（最常用）

```python
widget.setStyleSheet("background: magenta;")
```

立刻看清这个控件实际占多大、在哪。调完删掉这行即可。

### 打印实际大小

```python
print(widget.size())   # QSize(140, 140)
```

### 常见问题

| 症状 | 最可能的原因 | 怎么查 |
|------|------------|--------|
| 控件看不见 | 大小为 0 或被其他控件盖住 | 加背景色 `print(size)` |
| 位置不对 | 被外层布局影响 | 逐层加背景色排查 |
| QSS 不生效 | 写错了 / 被内联覆盖 | 先写内联测试语法 |
| 全叠在左上角 | 忘了设布局 | 检查 `setLayout` 或 `QVBoxLayout(parent)` |
| 内容被切掉 | 没包 QScrollArea | 把内容塞进 ScrollArea |
